import React from 'react';

const styles = {
  topBox: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    marginTop: 100,
  },
};

const Home = () => (
  <div style={styles.topBox}>
    Contents
  </div>
);

export default Home;
